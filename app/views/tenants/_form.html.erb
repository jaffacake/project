<%= form_for(@tenant) do |f| %>
  <% if @tenant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tenant.errors.count, "error") %> prohibited this tenant from being saved:</h2>

      <ul>
      <% @tenant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h2>Contact Details</h2>
  	<%= f.fields_for :contact_detail do |builder|%>
		<%= render 'contact_details/contact_details_fields', f: builder %>
	<% end %>
	
	
	
	<h2>Place Of Work</h2>
  	<%= f.fields_for :place_of_work do |builder|%>
		<%= render 'place_of_works/place_of_works_fields', f: builder %>
	<% end %>
	
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
