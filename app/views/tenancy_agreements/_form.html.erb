<%= form_for(@tenancy_agreement) do |f| %>
  <% if @tenancy_agreement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tenancy_agreement.errors.count, "error") %> prohibited this tenancy_agreement from being saved:</h2>

      <ul>
      <% @tenancy_agreement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<%= render 'tenancy_agreement_fields', f: f %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
