<h1>Front_End#index</h1>
<p>Find me in app/views/front_end/index.html.erb</p>
<% @estate_agent.each do |estate_agent| %>

<li><%= estate_agent.name -%> - <%= estate_agent.address.address_town -%></li>
<table>
  <tr>
  	<td><%= estate_agent.name %></td>
  	<td><%= estate_agent.address.address_town %></td>
  	<td><%= link_to 'Show', estate_agent %> | <%= link_to 'Edit', edit_estate_agent_path(estate_agent) %> | <%= link_to 'Destroy', estate_agent, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
</table>
<% end %>

<p><%= link_to "Advanced Search", '/searches/new' %></p>

