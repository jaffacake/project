<h1>Listing property_styles</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @property_styles.each do |property_style| %>
  <tr>
    <td><%= link_to 'Show', property_style %></td>
    <td><%= link_to 'Edit', edit_property_style_path(property_style) %></td>
    <td><%= link_to 'Destroy', property_style, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Property style', new_property_style_path %>
